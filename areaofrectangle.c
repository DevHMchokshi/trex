#include<stdio.h>
void main()
{
    double ans,width,length;
    printf("enter width:");
    scanf("%lf",&width);
    printf("enter length:");
    scanf("%lf",&length);
    ans=width*length;
    printf("The answer is : %lf",ans);
}